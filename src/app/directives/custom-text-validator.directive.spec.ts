import { CustomTextValidatorDirective } from './custom-text-validator.directive';

describe('CustomTextValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomTextValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
