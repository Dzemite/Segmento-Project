import { CustomZeroValidatorDirective } from './custom-zero-validator.directive';

describe('CustomZeroValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomZeroValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
